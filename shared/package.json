{
  "name": "@hapi/shared",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "db:migrate:deploy": "npx prisma migrate deploy && npx prisma db push",
    "test": "vitest run",
    "test:watch": "vitest watch"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^7.8.0",
    "@prisma/client": "^7.7.0"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "prisma": "^7.7.0"
  }
}

{
  "name": "@hapi/shared",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "db:migrate:deploy": "dotenv -e ../.env -- sh -c 'npx prisma migrate deploy && npx prisma db push'",
    "db:populate": "dotenv -e ../.env -- sh -c 'tsx ./src/scripts/populateDb/populateDb.ts'",
    "test": "vitest run",
    "test:watch": "vitest watch",
    "lint": "eslint ."
  },
  "dependencies": {
    "@prisma/adapter-pg": "^7.8.0",
    "@prisma/client": "^7.7.0"
  },
  "devDependencies": {
    "@aws-sdk/client-s3": "^3.1128.0",
    "@types/command-line-args": "^5.2.3",
    "@types/node": "^25.6.0",
    "command-line-args": "^6.0.2",
    "prisma": "7.x",
    "prisma-erd-generator": "^3.2.1",
    "supabase": "^2.117.0"
  }
}
